/* Customer app - flows (Scan, Pay, Buy).
   Placeholder contract until the flows task lands. Exposes a single global: CustomerFlows */

(function () {
  "use strict";

  window.CustomerFlows = {
    scan: function () { return ""; },
    overlay: function () { return ""; },
    createActions: function () { return {}; }
  };
})();
